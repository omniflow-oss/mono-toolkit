FROM ghcr.io/graalvm/native-image-community:23.0

RUN microdnf install -y nodejs npm git curl bash && microdnf clean all

WORKDIR /workspace

RUN corepack enable
RUN corepack prepare pnpm@10.4.0 --activate

ENTRYPOINT ["mono-toolkit"]
